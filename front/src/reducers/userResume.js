export const initialState = {
  hopeWork: [],
  hopeRegionFisrt: {
    region1: null,
    region2: null,
  },
  hopeRegionSecond: {
    region1: null,
    region2: null,
  },
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
